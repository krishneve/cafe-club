"use client";
import { useState } from "react";
import { useParams } from "next/navigation";
import Link from "next/link";
import { CustomerShell } from "@/components/CustomerShell";
export default function Feedback() { const { slug } = useParams<{ slug: string }>(); const [r,setR]=useState(5),[c,setC]=useState(""),[m,setM]=useState(""); async function send(){const x=await fetch("/api/customer/feedback",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({rating:r,comment:c})});const j=await x.json();setM(j.message||j.error)} return <CustomerShell slug={slug} cafeName="Your café"><Link href={`/c/${slug}`} className="text-sm font-bold text-[var(--muted)]">← My wallet</Link><div className="card mt-5 p-7"><span className="badge">Private feedback</span><h1 className="mt-3 text-3xl font-black">How was your visit?</h1><p className="mt-2 muted">Your feedback goes to the café team, not other customers.</p><div className="mt-7 flex justify-center gap-1">{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setR(n)} className={`text-4xl ${n<=r?"":"opacity-20"}`} aria-label={`${n} stars`}>★</button>)}</div><textarea className="input mt-7 min-h-32" placeholder="What did you love? What could be better?" value={c} onChange={e=>setC(e.target.value)}/><button className="btn primary mt-4 w-full" onClick={send}>Send feedback</button>{m&&<div className="mt-4 rounded-xl bg-[#f1e4d3] p-3 text-center text-sm font-semibold">{m}</div>}</div></CustomerShell> }
