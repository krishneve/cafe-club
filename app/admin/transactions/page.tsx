 "use client";import{useEffect,useState}from"react";import {AdminShell} from "@/components/AdminShell";
export default function Transactions(){const[rows,setRows]=useState<any[]>([]),[email,setEmail]=useState(""),[amount,setAmount]=useState(""),[items,setItems]=useState(""),[m,setM]=useState("");async function load(){setRows(await(await fetch("/api/cafe/transactions")).json())}useEffect(()=>{load()},[]);async function add(){const r=await fetch("/api/cafe/transactions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,amount:+amount,items})});const j=await r.json();setM(j.message||j.error);if(r.ok){setEmail("");setAmount("");setItems("");load()}}return <AdminShell><div><span className="badge">POS companion</span><h1 className="mt-3 text-3xl font-black">Transactions</h1><p className="mt-1 text-[#7c6d63]">Record an order without replacing your existing POS. Loyalty updates automatically.</p><div className="mt-6 grid gap-6 lg:grid-cols-[.65fr_1.35fr]"><div className="card p-6 grid gap-3"><h2 className="font-black">Record customer order</h2><input className="input" placeholder="Customer email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="number" placeholder="Amount ₹" value={amount} onChange={e=>setAmount(e.target.value)}/><input className="input" placeholder="Items / notes" value={items} onChange={e=>setItems(e.target.value)}/><button className="btn primary" onClick={add}>Save order + loyalty</button>{m&&<div className="rounded-xl bg-[#f1e4d3] p-3 text-sm">{m}</div>}</div><div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-[#fbf6ee]"><tr><th className="p-4">Order</th><th className="p-4">Customer</th><th className="p-4">Amount</th><th className="p-4">Loyalty</th><th className="p-4">Date</th></tr></thead><tbody>{rows.map(x=><tr className="border-t border-[#eadfd2]" key={x.id}><td className="p-4 font-bold">{x.orderNumber}</td><td className="p-4">{x.customer.name||x.customer.email}</td><td className="p-4 font-black">₹{x.amount}</td><td className="p-4">+{x.pointsEarned} pts · +{x.stampsEarned} stamp</td><td className="p-4">{new Date(x.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></div></div></AdminShell>}