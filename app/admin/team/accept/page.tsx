"use client";
import {useState} from "react"; import {useSearchParams,useRouter} from "next/navigation";
export default function Accept(){const q=useSearchParams(),router=useRouter(),[name,setName]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);const r=await fetch("/api/team/accept",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:q.get("token"),name})});const d=await r.json();if(!r.ok){setError(d.error||"Unable to accept invitation.");setBusy(false);return}router.push(d.redirect)};return <main className="grid min-h-screen place-items-center bg-[var(--cream)] p-5"><form onSubmit={submit} className="card w-full max-w-md p-7"><div className="badge">Team invitation</div><h1 className="mt-4 text-3xl font-black">Join the café team.</h1><p className="mt-2 text-sm leading-6 muted">Accept your invitation and you'll get access based on your assigned role.</p><label className="mt-7 block text-xs font-black">Your name<input value={name} onChange={e=>setName(e.target.value)} required className="input mt-2" placeholder="Alex Morgan"/></label>{error&&<div className="mt-4 rounded-2xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<button disabled={busy} className="btn primary mt-5 w-full">{busy?"Joining…":"Accept invitation"}</button></form></main>}
