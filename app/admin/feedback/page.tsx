import{AdminShell}from"@/components/AdminShell";import{cafeAdmin}from"@/lib/auth";import{db}from"@/lib/db";export default async function Feedback(){const s=await requirePermission("FEEDBACK");const r=await db.feedback.findMany({where:{cafeId:s.user.cafeId!},include:{customer:true},orderBy:{createdAt:"desc"}});return <AdminShell><span className="badge">Voice of customer</span><h1 className="mt-3 text-3xl font-black">Feedback</h1><p className="mt-1 text-[#7c6d63]">Private feedback gives the owner a recovery signal before a public review.</p><div className="mt-6 grid gap-3">{r.map(x=><div className="card p-5" key={x.id}><div className="flex justify-between"><b>{x.customer.name||x.customer.email}</b><span>{"★".repeat(x.rating)}</span></div><p className="mt-3 text-sm text-[#75665c]">{x.comment||"No comment"}</p></div>)}</div></AdminShell>}